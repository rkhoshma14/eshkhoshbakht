# خوشبخت — پس‌زمینه سه‌بعدی تونل / شبکه (v4)

## ایده
به‌جای افکت دکمه، یک **صحنه سه‌بعدی ثابت** در پس‌زمینه:
- حلقه‌های تونل (استعاره از VPN Tunnel)
- نودهای شناور (پروکسی / اندپوینت)
- گرید پرسپکتیو + پرتو نور
- واکنش به **ماوس** (چرخش ملایم) و **اسکرول** (parallax عمق)

مرتبط با محصول است، نه تزئین بی‌ربط.

## فایل‌های جدید / تغییر کرده
| فایل | نقش |
|------|-----|
| `static/style.css` | استایل صحنه ۳بعدی + حفظ طراحی قبلی |
| `static/panel.html` | ظرف صحنه + اسکریپت |
| `static/scene3d.js` | **جدید** — parallax ماوس/اسکرول (سبک، بدون تداخل با app.js) |
| `panel_pages.py` | همان صحنه روی صفحه لاگین |

## حفظ شده
- تمام قابلیت‌ها و API
- تمام کلاس‌های UI و منطق `app.js`
- RTL، موبایل، مینی‌اپ
- `prefers-reduced-motion` → صحنه ثابت می‌شود

## ربات تلگرام
پس‌زمینه سه‌بعدی داخل چت تلگرام ممکن نیست.
مینی‌اپ و پنل وب هر دو صحنه را دارند.

## نصب
جایگزین کن:
1. `static/style.css`
2. `static/panel.html`
3. `static/scene3d.js`  ← فایل جدید
4. `panel_pages.py`

دیپلوی + Hard Refresh (`Ctrl+Shift+R`)
